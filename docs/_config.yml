# GitHub Pages configuration
title: FreeResume AI
description: Create professional resumes with AI
# Actual hosting: GitHub Pages project site (no custom domain configured).
# If you later point freeresumeai.app here, update url back to it + clear baseurl.
url: "https://haider484991.github.io"
baseurl: "/cvmakermobapp-"

# Build settings
markdown: kramdown

# Don't publish internal engineering notes that live alongside the site.
exclude:
  - paywall
  - analytics
