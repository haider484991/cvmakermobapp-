# Paywall Debug Log — 2026-05-24

## Symptom

All v1.8.0/v1.8.1 users get "Failed to query product" when tapping a premium tier on the paywall. Paywall shows the hardcoded mock prices ($2.99/$19.99/$29.99), confirming that `fetchProducts()` returns an empty array for all SKUs.

## Confirmed working

- v1.8.0 + v1.8.1 builds on Production (versionCode 24, 25) and Internal Testing tracks
- All 3 products created and ACTIVE worldwide in Play Console:
  - `freeresume_premium_monthly` — $2.99/mo, 3-day trial
  - `freeresume_premium_annual` — $19.99/yr, 7-day trial
  - `freeresume_premium_lifetime` — $29.99 one-time
- Subscriptions visible in Play Console UI
- `com.android.vending.BILLING` permission verified in expo-iap config plugin
- Monetization Setup all green
- License tester set: haider484991@gmail.com
- 6 unique real-user devices in analytics
- Both v1.8.0 and v1.8.1 builds bundle openiap-google 2.2.1 (current)

## Failure pattern (from analytics)

Every v1.8.1 device shows:
```json
{
  "event_name": "paywall_offerings_loaded",
  "properties": {
    "count": 0,
    "using_mocks": true,
    "raw_sub_count": 0,
    "raw_sub_skus": "",
    "raw_onetime_count": 0,
    "raw_onetime_skus": ""
  }
}
```

No exception thrown — just empty results.

## Tested + ruled out

| Hypothesis | Test | Result |
|---|---|---|
| Pakistan-specific issue | Tested from device in different region | Same failure |
| Developer account block | Tested with non-dev Google account | Same failure |
| OnePlus 8 Pro specific | Tested on 2nd device | Same failure |
| Sideloaded APK | All installs via Play Store | All fail |
| Stale Play Store cache | Multiple cache clears, fresh installs | All fail |
| App signing fingerprint via SHA1 | All bundles unique sha1 hashes | OK |
| Product type mismatch | API config uses 'subs' / 'in-app' correctly | OK |
| `initConnection` failure | v1.8.1 tracks this — no init_failed events | OK (connection succeeds) |

## Most likely remaining causes (ranked)

### 1. Product propagation timing (HIGH probability)

Google's billing service indexes products on a different cycle than Play Console UI. Products created via the Android Publisher API can take **24–48 hours** to become queryable from the BillingClient even when the UI shows them as ACTIVE.

Products were created today at ~10:00 UTC. Earliest reasonable retry: **2026-05-25 ~10:00 UTC**.

### 2. App signing key mismatch (MEDIUM probability)

If the APK's signing certificate doesn't match what's registered for the app's billing products in Play Console, billing silently returns empty results.

**To verify:**
- Play Console → **Setup → App integrity** → "App signing key certificate" SHA-1
- Compare to what Play Console expects under "Manage your in-app products and subscriptions"

Both should match.

### 3. Products need UI "Edit + Save" nudge (LOW probability)

User completed this for subscriptions but may not have for the lifetime in-app product. Untested.

## Next steps for tomorrow

1. Run `node scripts/analytics-kpis.mjs --live`
2. Have user tap paywall
3. Check whether `paywall_offerings_loaded` now shows `count: 3, using_mocks: false`

If still 0 after 24h:
1. Verify App signing cert in Play Console
2. Test with a brand-new test account in a non-Pakistan region
3. If still failing, file a Google Play Support ticket:
   - https://support.google.com/googleplay/android-developer/contact/contact_support
   - "Monetization & Subscriptions" → "Products configured but BillingClient returns empty results"
   - Provide: package name `com.freeresumeai.app`, product IDs (all 3), evidence of API queries showing products active

## Files touched this debug

- `src/services/purchases/purchases.ts` — added `paywall_offerings_loaded`, `paywall_offerings_failed`, `purchases_init_success`, `purchases_init_failed` analytics events
- `scripts/play-console-diagnose.mjs` — created
- `scripts/play-console-activate-lifetime.mjs` — created (fixed DRAFT state)
- `scripts/play-console-worldwide.mjs` — created (made products available globally)
